version: '3'

services:
  postgres:
    container_name: postgres
    hostname: postgres
    image: postgres:16.0
    restart: always
    volumes:
      - ./data:/var/lib/postgresql/data
    env_file:
      - .env

networks:
  default:
    name: traefik_proxy
    external: true
